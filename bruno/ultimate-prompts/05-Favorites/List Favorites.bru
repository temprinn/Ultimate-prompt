meta {
  name: List Favorites
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/favorites
  body: none
  auth: bearer
}

auth:bearer {
  token: {{freeToken}}
}

assert {
  res.status: eq 200
  res.body.ok: eq true
}
