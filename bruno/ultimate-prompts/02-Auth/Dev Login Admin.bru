meta {
  name: Dev Login Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/dev-login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "role": "admin"
  }
}

script:post-response {
  const body = res.getBody();
  if (body?.data?.token) {
    bru.setEnvVar("adminToken", body.data.token);
  }
}

assert {
  res.status: eq 200
  res.body.ok: eq true
}
